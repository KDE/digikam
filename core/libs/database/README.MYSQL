TODO List
---------

- To re-init mysql database server under Linux with a clean intall, process as below :

  # su
  # rm -rf /var/lib/mysql
  # systemctl restart mysqld
  # mysql
    ...
    Enter SQL code to Mysql prompt in order to init digiKam databases with grant privileges (see previous point).
    ...
    quit
