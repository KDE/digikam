 * Fix thumbnails
   o and EV computation (most likely not triggered because of the missing thumbnail)
 * Change the default name to match the panorama tool/Hugin convention, appending "-enfuse" at the end
