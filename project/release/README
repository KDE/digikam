This is the release script wizard.

You need to install these packages:
- ruby

You first need to modify the config.ini file and change the version.

To create the tarball you need to do : 

ruby create_tarball.rb --noaccount --application digikam
